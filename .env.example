# Copy these into Vercel's Environment Variables, NOT into a real .env file in this repo.
# These values are safe to expose in the browser — they're the ANON key with read-only RLS,
# NOT the service-role key. Never commit a service-role key to GitHub.

VITE_SUPABASE_URL=https://xwjjrxdsegakshpxzfhi.supabase.co
VITE_SUPABASE_ANON_KEY=paste-your-anon-key-in-vercel
